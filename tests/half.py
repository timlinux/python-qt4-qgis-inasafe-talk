# coding=utf-8
"""A few simple functions to demonstrate best practices."""


def half(number):
    """Compute half the input.

    :param number: A number to be halved.
    :type number: int, float
    :rtype : float
    """
    new_no = float(number) / 2
    return new_no
